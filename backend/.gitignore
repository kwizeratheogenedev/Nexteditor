*.log
.env
# Backup copy of .env from before the ngrok removal - contains live secrets.
.env.before-ngrok-removal
uploads/*
!uploads/.gitkeep
clips/*
!clips/.gitkeep
node_modules/
whisper/
# Local OAuth token storage (YouTube upload) - never commit real tokens.
data/
